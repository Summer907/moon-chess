# MoonChess 优化交付记录

本轮完成请求一致性、有限步数 AI、进程内资源保护、三页面重设计和自动化验证。未部署、未增加数据库或多人模式。验证日期：2026-09-06。

## 行为与接口

- 每局返回 `revision`，初始为 0。成功落子、AI 落子、悔棋（包括空棋局）和重置均递增；查询和提示不递增。悔棋不回退版本。
- `moves`、`ai-move`、`undo`、`reset` 请求可带 `expected_revision`。不匹配返回 409，响应为 `{"detail":{"code":"state_conflict","params":{"revision":当前版本}}}`。旧客户端可以省略该字段，但没有普通操作的乐观并发保护。
- `undo` 可传 `steps: 1 | 2`，默认 1；无请求体仍兼容。两步在同一对局锁内完成；不足两步时撤销现有全部历史。
- AI 取快照后释放对局锁进行搜索，提交前重新检查快照版本。即使旧客户端不带版本，旧 AI 结果也不能覆盖重置、悔棋或其他落子。
- 前端对修改请求发送版本，普通请求 15 秒超时、AI 请求 30 秒超时。不会自动重发修改操作；响应不确定时读取服务端状态，读取失败则禁用落子并提供同步按钮。
- 明确失败的创建和 AI 请求有重试入口；遵循 Retry-After 倒计时。跨源响应公开 Retry-After 和 RateLimit 响应头。
- 重新开始复用当前对局的 reset 接口，避免反复重开占满个人容量。换边仅在重置成功后应用。正在提交时禁用重开、换边与悔棋；AI 延迟期间允许悔棋。

示例（对局已经落过两步，revision 为 2）：

```json
{"expected_revision": 2, "steps": 2}
```

POST 到 `/api/games/{id}/undo` 后返回 move_number 为 0、revision 为 3 的最终局面。

## 对局生命周期与运维

- 保留单进程内存存储。重启会失去对局，不能直接启动多个 worker 或横向扩容。
- 每 60 秒按请求触发批量清理；访问目标对局时即时检查 TTL 边界。
- 活动引用覆盖持锁、等待锁和 AI 搜索期间，避免正在使用的对局被淘汰。
- 容量满时只淘汰未使用的已结束或过期对局；不淘汰未过期进行中的对局。个人容量返回 `owner_capacity_reached`（429），全局容量返回 `game_capacity_reached`（503）。
- 新增 JSON 日志记录请求路由模板、状态码、耗时、AI 节点与命中数，以及冲突、限流和容量错误；这些事件不包含原始 IP 或完整棋谱。若不需要 Uvicorn 自带访问日志，可用 `--no-access-log` 启动。
- 原有规则、14 手上限与胜利优先顺序保持不变；路径重复不再提前判和。

## 界面

- 克制的月夜配色，深浅主题共用变量。首页保留模式身份与两条清晰入口；外部链接移至页脚。
- 对战和推演共用请求生命周期、显示偏好和响应式面板。全部辅助默认开启，按模式保存至原有 sessionStorage 偏好。
- 集中回合状态、双方图例、14 手进度、上一手标记与明确消子标识；根据服务端历史显示最新事件。
- 棋盘旁常驻悔棋与重新开始，已有历史时使用应用内确认；胜利、平局均展示结果与棋谱入口。
- 手机紧凑标题与单列布局；桌面独立棋盘列避免被设置高度撑开。390 × 844 默认中文页面可以完整显示棋盘和主要操作。
- 弹窗支持焦点约束与恢复，指南标签支持方向键/Home/End；棋格读屏标签包括辅助状态。保留减少动画偏好。
- 主要颜色变量的普通文字/表面组合测得最低对比度：深色 7.64:1、浅色 5.86:1。该检查不等同于完整 WCAG 认证。

## 验证

| 检查 | 结果 |
| --- | --- |
| `uv run pytest -q` | 32 通过 |
| `npm test`（frontend） | 8 通过 |
| `npm run build`（frontend） | 类型检查与生产构建通过 |
| `npm run test:e2e`（frontend） | 11 通过 |

覆盖：旧 AI 与重置/悔棋交错、版本冲突、原子两步悔棋、TTL/活动保护、明确空棋子参数、规则转换差分、抽样残局独立参考求解器、冷/热缓存一致性、响应丢失与重新同步、卸载后旧响应、AI 延迟与提交期操作、失败重试、换边确认、胜利与平局、主题/语言/键盘及三种窗口尺寸。

参考求解器通过公共规则引擎独立搜索有限剩余步数；当前对照测试为确定性抽样，不宣称穷举所有可达棋局。

窗口矩阵为 390 × 844、768 × 1024、1366 × 768，覆盖中英文和深浅主题。测试输出截图到 `artifacts/ui/`；失败跟踪位于 `frontend/test-results/`。这些生成文件不提交 Git，CI 上传为 browser-verification 工件。

本地测试存在一条依赖弃用提示：Starlette TestClient 使用 httpx 的兼容路径。所有测试仍通过；本轮未为消除提示而迁移 HTTP 测试依赖。

## 性能基线

本机 Windows / Python 3.12，单次样本，用于回归参考而非服务延迟保证。复现：`uv run python scripts/benchmark_ai.py`。

| 局面 | 缓存 | 秒 | 节点访问 | 命中 |
| --- | --- | ---: | ---: | ---: |
| 开局 | 冷 | 1.815182 | 412803 | 285970 |
| 开局 | 热 | 0.001621 | 9 | 9 |
| 第 6 手 | 冷 | 0.093354 | 10988 | 3590 |
| 第 6 手 | 热 | 0.001444 | 4 | 4 |
| 第 11 手 | 冷 | 0.002192 | 20 | 0 |
| 第 11 手 | 热 | 0.001787 | 4 | 4 |

前端体积使用 Vite 报告的十进制 kB：

| 资源 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 主入口 JS | 203.64 kB / gzip 72.37 kB | 177.77 kB / gzip 66.31 kB |
| CSS | 47.55 kB / gzip 10.40 kB | 12.93 kB / gzip 3.57 kB |
| 三个 favicon 合计 | 407.42 kB | 8.71 kB |

页面已按路由拆分。优化后还包含按需加载的首页 2.27 kB、对战页 4.78 kB、推演页 6.66 kB，以及共用游戏组件 23.00 kB；主入口减少不代表所有 JS 总量同比减少。图标复用原 ICO 内已有的 32 像素图像帧，未重新绘制。

## 开发与 CI

```sh
uv sync --locked --python 3.12
uv run pytest -q
cd frontend
npm ci
npm test
npm run build
npx playwright install chromium
npm run test:e2e
```

CI 使用 Python 3.12、Node 24，锁定依赖安装后执行以上检查。端到端测试自行启动 8018 端口的独立 API 进程并仅在该进程提高测试限额；8018 端口需空闲。GitHub 工作流已配置，本轮仅执行了本地检查，没有触发远端 CI 或生产部署。
